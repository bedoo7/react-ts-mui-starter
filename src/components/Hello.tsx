import { Button, Typography } from "@mui/material";

const Hello = () => {
  return (
    <>
      <Typography variant="h4">React + TS + MUI 🔥</Typography>
      <Button variant="contained">Click me</Button>
    </>
  );
};

export default Hello;
